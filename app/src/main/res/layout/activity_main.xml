<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/gradient_background"
    tools:context=".MainActivity">

    <!-- Circular ProgressBar -->
    <ProgressBar
        android:id="@+id/progress_circular"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:indeterminate="false"
        android:progressDrawable="@drawable/circular_progress"
        android:max="100"
        android:progress="0"
        style="@android:style/Widget.DeviceDefault.Light.ProgressBar.Horizontal"/>

    <!-- Imagen de gotita -->
    <ImageView
        android:id="@+id/drop_image"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginTop="-150dp"
        android:src="@drawable/gotita"
        android:contentDescription="Gotita de agua" />

    <!-- Texto del porcentaje -->
    <TextView
        android:id="@+id/humidity_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="0%"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@android:color/black" />

    <!-- Botón deslizante para conexión Bluetooth -->
    <ToggleButton
        android:id="@+id/bluetooth_toggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="100dp"
        android:backgroundTint="@color/teal_200"
        android:textOff="Conectar Bluetooth"
        android:textOn="Desconectar Bluetooth" />

</LinearLayout>
